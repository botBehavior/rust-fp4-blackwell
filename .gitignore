# Rust / cargo build outputs
target/
# cuda-oxide build artifacts under examples (regenerated by the backend)
**/*.ll
**/*.opt.ll
examples/**/*.ptx
examples/**/*.target
# raw probe logs (regenerate via probe.sh; SUMMARY.txt is kept)
probe-logs/*.log
# figure QA rasters (committed figures are PDFs)
figs/*.png
__pycache__/
*.pyc
# LaTeX build artifacts
*.aux
*.log
*.out
*.bbl
*.blg
*.synctex.gz
.DS_Store
